apiVersion: v1
kind: Service
metadata:
  name: demo-app-vulnerable
  namespace: vulnerable
spec:
  selector:
    app: demo-app-vulnerable
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
